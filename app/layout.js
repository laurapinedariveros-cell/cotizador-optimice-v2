export const metadata = {
  title: "Levantamiento de información — Cotizaciones",
  description: "Formulario interno de levantamiento de información para cotizaciones",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
